package java10;
/**
 * 
 * @author masahiro
 * P340の連番クラスに、最後に与えた識別番号を返すクラスメソッドを追加せよ。
 * インスタンスをn個生成した時点で呼び出すとnを返すこと
 *
 */

/* ====================================================================== */
/**
 * @brief 
 * 連番クラス
 *
 * @note
 * 
 */
/* ====================================================================== */

public class Id {
	
	/* ====フィールド==== */
	static int counter = 0; //何番までの識別番号を与えたか
	private int id; //識別番号

	/* ====コンストラクタ==== */
	public Id(){
		//インスタンス生成ごとにカウンタを加算する。
		id = ++counter;
	}
	
	/* ====================================================================== */
	/**
	 * @brief 
	 * 識別番号を取得するメソッド
	 *
	 * @param なし
	 *
	 * @return 
	 * 
	 *
	 * @note
	 */
	/* ====================================================================== */
	public int getId(){
		//識別番号を返却
		return id;
	}
	
	/* ====================================================================== */
	/**
	 * @brief 
	 * 最後に与えた識別番号を取得するメソッド
	 *
	 * @param なし
	 *
	 * @return 
	 * 
	 *
	 * @note
	 * インスタンスをn個生成した時点で呼び出すとnを返すこと
	 */
	/* ====================================================================== */
	public static int getMaxId(){
		//最後に与えた識別番号を返却
		return counter;
	}
	
}
